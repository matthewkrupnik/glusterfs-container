glusterfs:
  name: glusterfs
  image: matthewkrupnik/glusterfs:latest
  restart: always
  privileged: true
  network_mode: host
  volumes:
    - /mnt/glusterfs/gv0:/srv/glusterfs/gv0
    - /mnt/glusterfs/var/lib/glusterd:/var/lib/glusterd
